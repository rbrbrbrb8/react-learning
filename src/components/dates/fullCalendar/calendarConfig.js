const headerToolbar = {
  start:'listYear,dayGridMonth',
  center:'title',
  end:'today prev,next'
};

const buttonText ={
  list:'רשימה שנתית',
  month:'לוח שנה'
};


module.exports = {
  toolbarConfig:headerToolbar,
  textConfig:buttonText
}